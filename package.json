{
  "devDependencies": {
    "typescript": "^4.2.4"
  },
  "scripts": {
    "build": "tsc --outDir ./build",
    "run": "yarn run live-server"
  },
  "dependencies": {
    "live-server": "^1.2.1"
  }
}